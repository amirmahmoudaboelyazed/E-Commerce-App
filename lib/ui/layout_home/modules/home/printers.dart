import 'package:flutter/material.dart';

class PrintersProducts extends StatelessWidget {
  const PrintersProducts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(
        children: const [],
      ),
    );
  }
}
