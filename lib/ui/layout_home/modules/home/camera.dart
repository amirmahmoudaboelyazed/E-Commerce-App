import 'package:flutter/material.dart';

class CameraProducts extends StatelessWidget {
  const CameraProducts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(
        children: const [],
      ),
    );
  }
}
