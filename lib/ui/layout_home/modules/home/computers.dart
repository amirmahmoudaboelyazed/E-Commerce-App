import 'package:flutter/material.dart';

class ComputersProducts extends StatelessWidget {
  const ComputersProducts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(
        children: const [],
      ),
    );
  }
}
